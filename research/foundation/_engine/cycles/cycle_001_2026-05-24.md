# Cycle 001 — A1 family + cube-data dimensions

**Cycle window**: 2026-05-23 → 2026-05-24 (Cycle 002 spawned as proposals only).
**Engine spec**: `../ENGINE_SPEC.md` v0.
**Plan companion**: `../../PHASE_1_PLAN.md` v1 (`e87f9df`) — supersedes the standalone document; this file is now canonical.

---

## Selection (from `hypothesis_registry.yaml`)

| id | type | owner | prior Pr(VALIDATE) |
|---|---|---|---|
| A1 | cross_sectional | Claude | 0.20 |
| H2 | event_driven (cluster_buy long) | Codex | 0.25 |
| H3 | event_driven (mass_exit avoid) | Claude | 0.30 |
| H4 | cross_sectional (buy-intensity) | Claude | 0.10 |

Combined Pr(≥1 VALIDATE): ~0.55.

## Attack registry as of cycle start

B1, B2, B3-i, B3-ii, B3-iii, B4, B5-i, B5-ii, B7, D1 (all of attack_registry.yaml).

## Deliverables (from PHASE_1_PLAN.md §4 + §9, restated as engine artifacts)

- `research/foundation/strategies_a1.py` ← Claude
- `research/foundation/strategies_h2_cluster_buy.py` ← Codex
- `research/foundation/strategies_h3_mass_exit.py` ← Claude
- `research/foundation/strategies_h4_buy_intensity.py` ← Claude
- `research/smart_consensus/cube_events.py` (shared event extractor) ← Claude
- `research/foundation/run_all_hypotheses.py` ← Codex
- `research/foundation/cycle001_matched_baseline.py` ← Codex
- `research/smart_consensus/verdict_2026-05-24_foundation.md` (4 TL;DRs) ← Codex
- Retraction banner on `verdict_2026-05-23.md` ← Claude
- PR #6 update/retraction comment posted ← Codex, https://github.com/Soli22de/Bili_Stock/pull/6#issuecomment-4527438379

## Cycle kill criteria (engine §6, restated)

Per hypothesis:
1. self_test 7/7 must hold for the data state used (re-run by Codex on 2026-05-24 at closure: PASS).
2. Train and Test alpha must not disagree in sign.
3. Ablation: removing any single attack-registry mitigation can NOT restore >=80% of original artifact magnitude.
4. Net annualized alpha after survivorship discount and cost ≥ 0 → at minimum INCONCLUSIVE, < 0 → REJECT.
5. Soft: annualized alpha > 10% → MORE bug suspicion, demand additional ablation.

## Cycle budget

- Wall time: 24 hours from sign-off.
- Token budget: 5M total across both agents. Self-report at each commit.
- Hard stop if either exceeded.

## Spawn rules for Cycle 002

After all 4 cycle 001 verdicts land:
- If A1 / H2 / H3 / H4 each VALIDATED → propose deeper variants (size-neutral, industry-neutral, multi-factor combinations) for Cycle 002.
- If all REJECTED (the expected outcome at ~50%) → record in `negative_log.md` as "skill axis exhausted on cubes data: snapshot, rolling, static-holding, event-flow, intensity all failed".
- **DEFAULT spawn** (per Codex 2026-05-24 + Claude reply): regardless of outcome, Cycle 002 selects:
  1. **H5 (Behavioral Adverse Selection)** — Codex's reformulation, abandons skill axis, picks cubes by ex-ante behavior pattern + matched control. Owner: Codex.
  2. **I-B1 可转债套利** — first canonical catalog entry from `inefficiency_hunting.md`. Owner: Claude.
  Parallel execution; different data + code paths, no contention.
- Pull at least 3 additional external proposals via WebSearch / arxiv / `~/jz_code/research_log/` to fill remaining cycle slots.

## /goal launch shape (locked, per Codex)

```text
/goal until 'cycle_001 verdicts committed per ENGINE_SPEC §6, including foundation self_test 7/7, A1/H2/H3/H4 verdict files, size/liquidity-matched random baselines, retracted old verdict banner, PR #6 update draft reviewed by both agents, and cycle_002 proposals spawned from canonical catalog plus any surviving cubes anti-signal mechanisms; if any hard gate fails, write STOP in _sync/control.md and exit'
```

This is a bounded cycle executor, NOT an open-ended alpha hunt. Engine is the long-lived structure; /goal runs one cycle.

## Retrospective (filled 2026-05-24 at cycle close)

### Verdict summary

ALL 4 REJECT. Skill-axis hypothesis family on cubes data is exhausted.

| ID | n | α | t | Death pattern |
|---|---|---|---|---|
| A1 | 35 | +0.15% | +0.12 | Train/Test signs disagree + alpha ≈ 0 |
| H2 | 109 | +0.90% | +0.71 | Train/Test signs disagree + Full t < 1 |
| H3 | 111 | +0.33% | +0.29 | Wrong sign + Train/Test disagree |
| H4 | 35 | −1.17% | −0.99 | Negative full alpha + Test t=−2.70 wrong-way |

Canonical artifacts:
- `research/smart_consensus/verdict_2026-05-24_foundation.md` (combined verdict)
- `research/smart_consensus/output/cycle001_foundation_reports.md` (raw foundation output)
- `research/smart_consensus/output/cycle001_matched_baseline.md` (size/liquidity-matched supplemental baseline)
- `research/smart_consensus/verdict_2026-05-23.md` RETRACTED banner added
- `research/foundation/_sync/pr6_cycle001_update.md` (PR #6 update text; posted at https://github.com/Soli22de/Bili_Stock/pull/6#issuecomment-4527438379)

### What worked

- **Engine immune system held**: attack registry caught every hypothesis. No false-positive VALIDATE despite original A1 looking "VALIDATED_FOR_NEXT_STAGE" pre-audit.
- **Dual-agent cross-check**: Codex's ablation independently confirmed Claude's IC delta; he caught my duplicate H5 registry id; I committed his uncommitted H2 work when he went offline. Coordination via `_sync/` worked.
- **Bounded /goal stopping condition** (per Codex form): each hypothesis verdict was verifiable artifact, not "agent says done".
- **Codex review of PHASE_1_PLAN** added the `+1.0 + jitter` for zero-exposure (B7 mitigation). Without it A1 would have hidden tie-order bias too.

### What didn't

- Cycle 001 selected 4 hypotheses but ALL on the same axis. Codex's D1 phased reformulation came after — should have been pre-cycle.
- I (Claude) yes-manned Codex's framing once before Johnny intervened ("不能比 codex 怂"). The anti-yes-man clause now in claude_outbox is the durable fix.
- `fetch_fundamentals.py` did not pre-filter panel A-share only, causing self_test to fail until manual filter. Should be patched upstream in cycle 002.

### What we'd do differently in Cycle 002

1. **Pre-cycle axis-stability audit**: before selecting hypotheses, audit cohort rotation rate of the selection axis. If >20%/quarter, the axis is unstable enough to kill any hypothesis on it. This becomes attack registry B8 candidate (proposed in lessons_learned L9).
2. **Diverse axis selection**: cycle 002 should pick hypotheses on DIFFERENT axes (behavior vs canonical inefficiency), not all on one.
3. **Earlier registry hygiene**: id uniqueness check (H5/H7 issue) should be a CI lint on hypothesis_registry.yaml.

### Negative-log patterns observed

Train/Test alpha sign divergence in 3 of 4 (A1, H2, H3). Not coincidence — it's the canonical death signature for selection-axis instability. Captured as L10 in lessons_learned.

### Attack registry additions

- **B8 (proposed, not yet formalized)**: selection-axis instability test. Audit cohort rotation rate of any selection axis before testing hypotheses on it.

### Lessons_learned additions

- **L9** — Selection-axis instability is a category of bug above individual hypothesis bugs.
- **L10** — Train/Test sign divergence is the canonical death signature for axis instability.

### Sign-off

- Implementer (Claude): all 4 strategies committed, run_all_hypotheses.py end-to-end pass, verdicts in registry.
- Attacker (Codex): re-ran foundation self_test 7/7, run_all_hypotheses.py, and cycle001_matched_baseline.py on 2026-05-24; co-signs A1/H2/H3/H4 all REJECT.
- Arbitrator (Johnny): D1-D4 super-brain judgments delivered; engine spec updates them. Cycle 002 spawn awaits his launch authorization.

**Cycle 001 closed. Negative log carries forward to cycle 002.**
